package design_patterns.structural.adapter;

class UglyAndLowLevelOfImageTransformingAPI {

    public byte[] process(int ex, int ac, double col) {
        //some nasty stuff that we don't want to even look
        return new byte[] {};
    }

    public byte[] processAndChangeType(int ex, int ac, double col, String type) {
        //some nasty stuff that we don't want to even look
        return new byte[] {};
    }
}
